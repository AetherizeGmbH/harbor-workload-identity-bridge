{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      2,
      "always",
      ["feat", "fix", "perf", "refactor", "docs", "style", "test", "ci", "build", "chore", "revert"]
    ],
    "scope-empty": [0],
    "header-max-length": [2, "always", 100]
  }
}
